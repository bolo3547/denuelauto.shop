# Local development API base (backend server)
# Local development (non-Docker) uses http://localhost:4000
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# When running with docker-compose, the frontend will connect to the backend service by name
# NEXT_PUBLIC_API_BASE_URL=http://backend:4000
