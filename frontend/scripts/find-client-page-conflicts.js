const fs=require('fs');const path=require('path');function findFiles(dir){let out=[];for(const p of fs.readdirSync(dir,{withFileTypes:true})){const full=path.join(dir,p.name);if(p.isDirectory()) out=out.concat(findFiles(full)); else out.push(full);}return out}const files=findFiles('app').filter(f=>f.endsWith('page.tsx')||f.endsWith('page.jsx'));const conflicts=[];for(const f of files){const t=fs.readFileSync(f,'utf8'); if(/export\s+function\s+generateStaticParams\s*\(/.test(t) && /("use client"|'use client')/.test(t)) {conflicts.push(f);} } console.log('Conflicts', conflicts.length); conflicts.forEach(c=>console.log(c));